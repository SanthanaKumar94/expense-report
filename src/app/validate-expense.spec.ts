import { ValidateExpense } from './validate-expense';

describe('ValidateExpense', () => {
  it('should create an instance', () => {
    const directive = new ValidateExpense();
    expect(directive).toBeTruthy();
  });
});
